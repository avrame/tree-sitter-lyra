====================
Basic String Literal
====================

let greeting = "Hello, World!"

---

(program
  (declaration
    (identifier)
    (string_literal
      (string_content))))

=================================
String Literal with interpolation
=================================

let greeting = "Hello, #{name}"

---

(program
  (declaration
    (identifier)
    (string_literal
      (string_content)
      (string_interpolation
        (identifier)))))

======================================================
String Literal with a complex interpolation expression
======================================================

let greeting = "Hello, #{employee.get_name()}!"

---

(program
  (declaration
    (identifier)
    (string_literal
      (string_content)
      (string_interpolation
        (call_expression
          (member_expression
            (identifier)
            (identifier))
          (argument_list)))
      (string_content))))

=====================================
String Literal with escaped hash char
=====================================

"Phone \#: #{phone_num}"

---

(program
  (expression_statement
    (string_literal
      (string_content)
      (string_interpolation
        (identifier)))))

========================================
String Literal with nested interpolation
========================================

let greeting = "Hello, #{if is_male then "Mr. #{last_name}" else "Mrs. #{last_name}" end}!"

---

(program
  (declaration
    (identifier)
    (string_literal
      (string_content)
      (string_interpolation
        (if_then_expr
          (if_condition
            (identifier))
          (string_literal
            (string_content)
            (string_interpolation
              (identifier)))
          (else)
          (string_literal
            (string_content)
            (string_interpolation
              (identifier)))))
      (string_content))))

=============================================================
String interpolation with missing closing bracket is an error
:error
=============================================================

"Phone \#: #{phone_num"

---

(program
  (ERROR
    (string_content)
    (ERROR
      (identifier))))